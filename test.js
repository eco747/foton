import {
    DOMElement
} from "dom";


/*
import {
    setInterval,
    setTimeout
} from "timer";

import {
    console
} from "console";

let counter = 0;
setInterval( function() {
    console.log( ++counter );
}, 10 );
*/

let el = new DOMElement("div");
